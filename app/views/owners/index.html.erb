
<div class="owners-header">
  <h1>Propietaris</h1>
  <div><%= link_to "New owner", new_owner_path, data: { turbo_frame: "new_owner" } %></div>
</div>

<%= turbo_frame_tag "new_owner", target: "_top" %>

<div id="owners">
  <%= render @owners %>
</div>
