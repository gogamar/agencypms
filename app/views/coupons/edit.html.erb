<%# render 'form' %>
<div>
  <h1 class="font-bold text-2xl mb-3">Editing Coupon</h1>
  <turbo-frame id="<%= dom_id @coupon %>">
    <%= render "form" %>
    <%= link_to t('cancel'), company_office_coupons_path(@company, @office), class: "btn btn-sm btn-secondary" %>
  </turbo-frame>
</div>
