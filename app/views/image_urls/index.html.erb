<p style="color: green"><%= notice %></p>

<h1>Image urls</h1>

<div id="image_urls">
  <% @image_urls.each do |image_url| %>
    <%= render image_url %>
    <p>
      <%= link_to "Show this image url", image_url %>
    </p>
  <% end %>
</div>

<%= link_to "New image url", new_image_url_path %>
