<%# @vrental.features.each do |feature| %>
  <%# feature.name %>
<%# end %>

<% Feature.all.each do |feature| %>
<%= feature.name %>
<%= link_to 'Esborrar', feature, data: {turbo_method: :delete, turbo_confirm: "Segur que vols esborrar #{feature.name}?"} %>
<% end %>

<%# Feature.distinct.pluck(:name) %>
