<div class="container d-flex justify-content-end">
  <div class="d-flex align-items-center">
    <%= link_to new_rate_plan_path, class: "btn btn-light btn-icon-split" do %>
      <span class="icon text-white-600">
        <i class="fas fa-plus"></i>
      </span>
      <span class="text">
        Nou plà de tarifa <%= current_user.company.name %>
      </span>
    <% end %>
  </div>
</div>
<div id="rate_plans">
  <% @rate_plans.each do |rate_plan| %>
    <div class="row border m-3 text-center">
      <div class="col"><%= rate_plan.name %></div>
      <div class="col"><%= l(rate_plan.start, format: :standard) %> - <%= l(rate_plan.end, format: :standard) %></div>
      <div class="col"><%= link_to "Veure", rate_plan %></div>
      <div class="col"><%= link_to "Modificar", edit_rate_plan_path(rate_plan) %></div>
    </div>
  <% end %>
</div>
