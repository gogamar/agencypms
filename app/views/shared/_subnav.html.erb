<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to 'javascript:history.back()', class: "btn btn-light btn-icon-split me-3" do %>
      <span class="icon text-white-600">
        <i class="fas fa-arrow-left"></i>
      </span>
      <span class="text m-0 text-primary" style="font-size: 0.85rem">
        Tornar
      </span>
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-between" id="navbarNavDropdown">
      <ul class="navbar-nav">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="all_vrentals" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Immobles
          </a>
          <ul class="dropdown-menu" aria-labelledby="all_vrentals">
            <li><%= link_to 'Tots els immobles', vrentals_path, class: "dropdown-item" %></li>
            <li><%= link_to 'Afegir immoble', new_vrental_path, class: "dropdown-item" %></li>
          </ul>
        </li>
        <% if @vrental&.persisted? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="agreement" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Contractes
            </a>
            <ul class="dropdown-menu" aria-labelledby="agreement">
              <li>
                <% if @vrental.vrowner %>
                  <% if @vrental.status == "inactive" %>
                    <small><%= link_to "Canvia l'estatus de #{@vrental.name} a proposta o actiu per afegir contracte", edit_vrental_path(@vrental) %></small>
                  <% else %>
                    <% if @vrental.vragreements.pluck(:year).include?(Date.today.year) %>
                      <%= link_to "Afegir contracte #{Date.today.next_year.year} per #{@vrental.name}", new_vrental_vragreement_path(@vrental, year: Date.today.next_year.year), class: "dropdown-item" %>
                    <% elsif @vrental.vragreements.pluck(:year).include?(Date.today.next_year.year) %>
                      <small>El contracte <%= Date.today.next_year.year %> per <%= @vrental.name %> ja existeix</small>
                    <% else %>
                      <%= link_to "Afegir contracte #{Date.today.year} #{@vrental.name}", new_vrental_vragreement_path(@vrental, year: Date.today.year), class: "dropdown-item" %>
                    <% end %>
                  <% end %>
                <% else %>
                  <li>
                    <%= link_to "Afegeix propietari per a #{@vrental.name}", edit_vrental_path(@vrental), class: "dropdown-item" %>
                  </li>
                <% end %>
              </li>
              <li>
                <%= link_to "Tots els contractes #{@vrental.name}", vragreements_path(immoble: @vrental), class: "dropdown-item" %>
              </li>
            </ul>
          </li>
        <% end %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="finstatement" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Liquidació
          </a>
          <ul class="dropdown-menu" aria-labelledby="finstatement">
            <li><%= link_to 'Despeses tots immobles', expenses_path, class: "dropdown-item" %></li>
            <% if @vrental&.persisted? %>
              <li><%= link_to "Despeses #{@vrental.name}", vrental_expenses_path(@vrental), class: "dropdown-item" %></li>
              <li><%= link_to "Reserves #{@vrental.name}", vrental_bookings_path(@vrental), class: "dropdown-item" %></li>
              <li><%= link_to "Liquidacions #{@vrental.name}", vrental_statements_path(@vrental), class: "dropdown-item" %></li>
              <li><%= link_to "Factura Propietari #{@vrental.name}", vrental_bookings_path(@vrental), class: "dropdown-item" %></li>
            <% end %>
          </ul>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="beds24" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Beds24
          </a>
          <ul class="dropdown-menu" aria-labelledby="beds24">
            <% if @vrental&.persisted? %>
              <li><%= link_to "Crear #{@vrental.name} a Beds24", export_beds_vrental_path(@vrental), class: "dropdown-item" %></li>
              <li><%= link_to "Actualitzar #{@vrental.name} a Beds24", update_beds_vrental_path(@vrental), class: "dropdown-item" %></li>
            <% end %>
            <li><%= link_to 'Importar immobles', import_properties_vrentals_path, class: "dropdown-item text-danger" %></li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
