<!DOCTYPE html>
<html>
  <head>
    <title>Contractes</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%# in the old version of the app it was javascript_pack_tag %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= javascript_include_tag Ckeditor.cdn_url %>
    <%= favicon_link_tag asset_path('sistach_favicon.png') %>
    <%# don't know if turbo_include_tags is necessary %>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v6.1.2/css/all.css">
    <!-- CSS only -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.bundle.min.js"></script>
    <%= stylesheet_link_tag "application", media: 'all', "data-turbo-track": "reload" %>
  </head>

  <body id="page-top"
        data-controller="sidebar"
        data-sidebar-target="dash"
        <%# data-action="resize@window->sidebar#closeaccordions" %>
  >
    <%# render "shared/navbar" %>
    <!-- Page Wrapper -->
    <div id="wrapper">

      <% if controller_name != 'profile_sessions' && controller_name != 'sessions' %>
        <%= render "shared/sidebar" %>
      <% end %>
      <%= render "shared/flashes" %>
      <div id="content-wrapper" class="d-flex flex-column">
        <div id="content">
          <% if controller_name != 'sessions' %>
            <%= render "shared/topbar" %>
          <% end %>
          <div class="container-fluid">
            <%= yield %>
          </div>
        </div>
          <!-- Footer -->
          <footer class="sticky-footer bg-white">
            <div class="container my-auto">
              <div class="copyright text-center my-auto">
                <span>&copy; Sistach Finques <%= Date.today.year %></span>
              </div>
            </div>
          </footer>
          <!-- End of Footer -->
      </div>
      <!-- End of Content Wrapper -->
    </div>
    <!-- End of Page Wrapper -->
  </body>
</html>
