<!DOCTYPE html>
<html class="h-100">
  <head>
    <title>Sistach Rentals · Sistach Finques</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="turbo-visit-control" content="reload">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%# in the old version of the app it was javascript_pack_tag %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= javascript_include_tag Ckeditor.cdn_url %>
    <%= favicon_link_tag asset_path('sistach_favicon.png') %>
    <%# don't know if turbo_include_tags is necessary %>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v6.1.2/css/all.css">
    <!-- CSS only -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/js/bootstrap.bundle.min.js"></script>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>
  <body class="d-flex flex-column">
    <div id="page-content">
      <div id="wrapper">
        <div id="content-wrapper" class="d-flex flex-column">
          <%= render "shared/flashes" %>
          <p style="color: green"><%= notice %></p>
          <% if user_signed_in? %>
            <%= render "shared/topbar" %>
            <%= render "shared/navbar" unless devise_controller? %>
            <%= render "shared/subnav" if @vrental && @vrental.persisted? %>
          <% end %>
          <div id="content" class="flex-shrink-0">
            <div class="container-fluid my-5">
              <%= yield %>
            </div>
          </div>
        </div>
      </div>
    </div>
    <footer id="sticky-footer" class="flex-shrink-0 py-4 bg-dark text-white-50">
      <div class="container">
        <div class="copyright text-center my-auto">
          <span>&copy; <%= current_user.company&.offices&.first.name %> <%= Date.today.year %></span>
        </div>
      </div>
    </footer>
  </body>
</html>
