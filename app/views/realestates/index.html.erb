<div class="container-fluid">
  <div class="my-3 text-end">
    <%= link_to new_realestate_path, class: "btn btn-secondary btn-icon-split" do %>
      <span class="icon text-white-50">
        <i class="fas fa-house"></i>
      </span>
      <span class="text">
        Afegir un immoble de compravenda nou
      </span>
    <% end %>
  </div>
  <%= render "realestates", realestates: @realestates %>
</div>
